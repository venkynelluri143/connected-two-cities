package com.walmart.gga.service;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.transaction.annotation.Transactional;


import static org.junit.Assert.assertNotNull;

//@RunWith(SpringRunner.class)
//@SpringBootTest
//@TransactionConfiguration
//@ContextConfiguration(classes = { LocalDbConfig.class, InternationalDbConfig.class })
public class MultipledatasourcesApplicationTests {
	
/*	@Autowired
	CardRepository cardRepositroy;*/
	
	//@Test
    //@Transactional("tm2")
    public void create_check_card() {/*
		System.out.println("Response Expiration Year :");
        com.walmart.gga.international.dao.Card card = cardRepositroy.findByCardId(new Long(2));
		System.out.println("Response Expiration Year :"+card.getExpirationYear());
        //assertNotNull(cardRepositroy.findById(card.getId()));
        */}
	
	
}
