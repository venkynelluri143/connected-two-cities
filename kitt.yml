# Required minimal config
owner:
  group: corehr-wcnp-developers # change to your own group 
notify:
  slack:
    channelName: corehr-wcnp-hcm-nonprod #change to your own slack channel 
profiles:
  - springboot-web
setup:
  releaseRefs: ["featurebranch","GAI_SonarIntegration"]
# Needed to get container to start, depends on app  
deploy:
  namespace: getassoc-service
  releaseType:
   deployTimeout: 1200
  helm:
    values:
      livenessProbe:
        wait: 120
      readinessProbe:
        wait: 300    
      min:
        cpu: 100m
        memory: 256Mi
      max:
        cpu: 512m
        memory: 1024Mi
      networking:
        httpsEnabled: true
        httpEnabled: true 
  stages:
    - name: dev
      helm:
        values:
          env:
            JAVA_OPTS: "-Dspring.profiles.active=dev"
      refs: [master,branch]
      target:
        - cluster_id: [scus-dev-a2]
